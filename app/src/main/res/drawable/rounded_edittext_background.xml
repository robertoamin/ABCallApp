<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/gray_500"/> <!-- Cambia por el color de fondo deseado -->
    <corners android:radius="5dp"/> <!--Bordes redondeados -->
    <stroke android:width="2dp" android:color="@android:color/darker_gray"/> <!-- Borde opcional -->
</shape>
